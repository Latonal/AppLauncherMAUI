<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppLauncherMAUI.MVVM.Views.HomeView"
             xmlns:local="clr-namespace:AppLauncherMAUI.MVVM.ViewModels"
             xmlns:controls="clr-namespace:AppLauncherMAUI.MVVM.Views.Controls"
             x:DataType="local:HomeViewModel">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <CollectionView ItemsSource="{Binding AppCardList}"
                            ItemsLayout="HorizontalList">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="controls:AppCard">
                        <controls:AppCard AppCardId="{Binding AppCardId}" />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <StackLayout>
                <StackLayout Orientation="Horizontal">
                    <Label>Clicks:</Label>
                    <Label Text="{Binding ClicksReceived}"/>
                </StackLayout>
            </StackLayout>
            <Button Text="Click here" Command="{Binding ActionClickedCommand}" />

        </VerticalStackLayout>
    </ScrollView>

</ContentView>
